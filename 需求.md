1. 我刚入手了一只西高地，三个月，想做一个它的日常养育管理的app，以手机端为主，兼顾pc。
2. 主要功能：
    - 可以录入小狗基本信息
    - 持续跟踪体重等信息、增长曲线
    - 记录它的饮食、排便、如厕等日常情况，用一个时间日记模式，记录它的疫苗和驱虫情况
    - 可以设置提醒，比如几点喂食、几点带出去大小便等
    - 支持拍照记录
    - 能有美观的图表展示
    - 能有一些采购物品的费用记录和统计
    - 数据使用cloudflare存储，pc和移动端要同步


    - 我希望引入一个狗叫声检测和深度学习分析的机制，我可以手机长期运行本项目，然后监测到狗叫就记录，不断记录数据，校准优化模型，然后分析叫声音频向量等方法，预测判断狗可能是因为无聊想出去玩了，还是因为想吃东西了，还是因为什么。

    - 我现在不确定每条的含义，不用每一条都马上让我判断，你可以先全面的收集，通过音频对比embedding等等方法来聚类，建立一个库分析
    - 监测的灵敏度感觉不太行，很多明显的狗叫没有识别到，你看看有没有类似的库有资源，提升一下识别准确性
    - 希望能显示狗叫的音频
    - 优化手机端的信息密度，页面太松散了

    - 一个屏幕显示也不是顶部的都固定不动，这样下面时间轴都看不见了
- 组件有图片的时候高度不对
- 完全不是antidesign风格，原来的组件是不是都自己写的，用专业成体系的
- 要做成真正手机端可以用的界面
- 主页面、子页面都全部统一
- 提醒事项要支持跳过
- 首页的事件流要简洁，有详情按钮可以看具体，然后事件流应该分日期，可以切换，只显示当天就好
- 快捷事件所有压缩在一行
- 错误：▲ [WARNING] Using direct eval with a bundler is not recommended and may cause problems [direct-eval]

    .open-next/server-functions/default/handler.mjs:205:923:
      205 │ ...searchedLocations=[],dirname=eval("__dirname"),searchLocations...
          ╵                                 ~~~~

  You can read more about direct eval and bundling here: https://esbuild.github.io/link/direct-eval


(node:18757) [DEP0190] DeprecationWarning: Passing args to a child process with shell option true can lead to security vulnerabilities, as the arguments are not escaped, only concatenated.
(Use node --trace-deprecation ... to show where the warning was created)

- 一个屏幕显示也不是顶部的都固定不动，这样下面时间轴都看不见了
- 组件有图片的时候高度不对
- 完全不是antidesign风格，原来的组件是不是都自己写的，用专业成体系的
- 要做成真正手机端可以用的界面
- 主页面、子页面都全部统一
- 提醒事项要支持跳过
- 首页的事件流要简洁，有详情按钮可以看具体，然后事件流应该分日期，可以切换，只显示当天就好
- 快捷事件所有压缩在一行
- 错误：▲ [WARNING] Using direct eval with a bundler is not recommended and may cause problems [direct-eval]

    .open-next/server-functions/default/handler.mjs:205:923:
      205 │ ...searchedLocations=[],dirname=eval("__dirname"),searchLocations...
          ╵                                 ~~~~

  You can read more about direct eval and bundling here: https://esbuild.github.io/link/direct-eval


(node:18757) [DEP0190] DeprecationWarning: Passing args to a child process with shell option true can lead to security vulnerabilities, as the arguments are not escaped, only concatenated.
(Use node --trace-deprecation ... to show where the warning was created)